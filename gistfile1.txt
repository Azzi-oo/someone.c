def get_mean(values):
    return sum(values) / len(values) # Место для вашего кода

# Список значений для теста
num_lst = [3, 6, 5, 7, 9, 1]
mean = get_mean(num_lst)
# Тут вызов функции.
print(round(mean, 1))